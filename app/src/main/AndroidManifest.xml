<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Factusoft"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        
        <!-- LoginActivity como pantalla de inicio -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Factusoft">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- MainActivity ahora es la pantalla principal después del login -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity android:name=".InventoryActivity" />
        <activity android:name=".SalesActivity" />
        <activity android:name=".PurchasesActivity" />
        <activity android:name=".CustomersActivity" />
        <activity android:name=".SuppliersActivity" />
        <activity android:name=".ReportsActivity" />
        <activity android:name=".ProductCreateActivity" android:theme="@style/Theme.Factusoft.Modal" />
        <activity android:name=".CustomerCreateActivity" android:theme="@style/Theme.Factusoft.Modal" />
        <activity android:name=".CustomerEditActivity" android:theme="@style/Theme.Factusoft.Modal" />
        <activity android:name=".SupplierCreateActivity" android:theme="@style/Theme.Factusoft.Modal" />
        <activity android:name=".PurchaseCreateActivity" android:theme="@style/Theme.Factusoft.Modal" />
    </application>

</manifest>